# Keyra web — local development
#
# Copy to `.env` (gitignored) and adjust.

# Prisma — PostgreSQL (local Docker example below, Railway uses the Postgres plugin URL).
DATABASE_URL="postgresql://keyra:keyra@localhost:5432/keyra?schema=public"

# Optional: serve admin UI/API only on this hostname (e.g. admin.keyra.ie). When set, requests to
# /admin/* and /api/admin/* on www or apex are redirected here. Omit locally (use http://localhost:3030/admin).
# KEYRA_ADMIN_HOST="admin.keyra.ie"
# KEYRA_ADMIN_PUBLIC_ORIGIN="https://admin.keyra.ie"

# Optional: Nexa / Ciright analytics for admin (production on admin.keyra.ie and keyra.ie/admin).
# Set to 1 locally to verify the script on http://localhost:3030/admin — not for shared staging.
# KEYRA_ADMIN_ANALYTICS_FORCE="1"
# Optional: comma-separated hosts where /admin/* uses admin analytics (default: keyra.ie,www.keyra.ie).
# KEYRA_MARKETING_HOSTS="keyra.ie,www.keyra.ie"

# Admin: at least one of the following must be set for production.
# — JWT signing secret (min 32 chars). In development, a built-in dev secret is used if unset.
KEYRA_ADMIN_JWT_SECRET=""

# — Legacy break-glass shared secret (optional). If set, can be used on /admin/login in the
#   "Break-glass token" field to obtain a service JWT, or sent as raw Bearer/cookie for APIs.
KEYRA_ADMIN_TOKEN="change-me"

# Optional: actor labels written into audit/status history rows
KEYRA_ADMIN_ACTOR_ID="admin"
KEYRA_ADMIN_ACTOR_ROLE="Global Admin"

# Seed script (`npm run db:seed`): password for all demo AdminUser rows (min 8 chars recommended).
# If unset, seed uses ChangeMeSeed!123.
SEED_ADMIN_PASSWORD="ChangeMeSeed!123"

# Optional: used in server access verification emails
NEXT_PUBLIC_KEYRA_SITE_URL="http://localhost:3030"

# Global deployment map (governments subdomain). Links and /global-deployment redirect here.
NEXT_PUBLIC_GOVERNMENTS_URL="https://governments.keyra.ie"

# Local dev: phone used to establish keyra_session on localhost after Get Started login
# (production auth cookies cannot cross to localhost). Must match a seeded AdminUser phoneE164.
KEYRA_DEV_SESSION_PHONE="+919054223823"

# Marketing CMS origin — footer and other public content APIs (production: https://keyra.ie).
NEXT_PUBLIC_KEYRA_MARKETING_ORIGIN="https://keyra.ie"

# Optional: SimSecure auth API (production: https://auth.keyra.ie). Local dev can use /api/simsecure-auth with next.config rewrites.
# NEXT_PUBLIC_SIMSECURE_AUTH_BACKEND_URL="/api/simsecure-auth"

# Optional: Next.js 16 LAN dev — comma-separated hosts (e.g. 192.168.0.181) for phone testing on Wi‑Fi. Dev only.
# KEYRA_ALLOWED_DEV_ORIGINS="192.168.0.181"

# Skip agent worlds admin tab seed during full deploy catalog boot:
# SKIP_AGENT_WORLD_SEED=1
# Re-seed agent worlds tabs when catalog already exists:
# FORCE_AGENT_WORLD_SEED=1 npm run db:seed:agent-world
NEXT_PUBLIC_SOIP_URL="http://localhost:3060"

# App launcher (9-dot): same NEXT_PUBLIC_* names as `simsecure/components/BentoMenu.js`. Production defaults are *.keyra.ie.
# NEXT_PUBLIC_GET_STARTED_URL="https://get-started.keyra.ie"
# NEXT_PUBLIC_SIMSECURE_URL="https://app.keyra.ie"
# NEXT_PUBLIC_DEVELOPER_URL="https://developer.keyra.ie"
# NEXT_PUBLIC_MY_ACCOUNT_URL="https://myaccount.keyra.ie"
# NEXT_PUBLIC_SETTINGS_URL="https://setting.keyra.ie"
# NEXT_PUBLIC_AFFILIATES_URL="https://affiliate.keyra.ie"
# NEXT_PUBLIC_PRESS_URL="https://press.keyra.ie"
# Optional: override consumer hub for ElevenLabs / other widgets (else SIMSECURE_URL, else same-origin /app).
# NEXT_PUBLIC_KEYRA_APP_URL="https://app.keyra.ie"

# Strategic consultation (contact.keyra.ie, mycalendar.ciright.com, ve.keyra.ie)
# KEYRA_CONTACT_API_BASE_URL="https://contact.keyra.ie"
# KEYRA_CALENDAR_API_BASE_URL="https://mycalendar.ciright.com"
# KEYRA_VE_API_BASE_URL="https://ve.keyra.ie"
# KEYRA_CONTACT_API_KEY=""
# KEYRA_CALENDAR_API_KEY=""
# KEYRA_VE_API_KEY=""
# KEYRA_CONSULTATION_NOTIFY_EMAIL="advisory@keyra.ie"
# KEYRA_CONSULTATION_DEV_MOCK="1"

# Optional: Mailchimp Transactional (Mandrill) for access-request emails
# MANDRILL_API_KEY=""
# MANDRILL_FROM_EMAIL=""
